name: Waka Readme

on:
  schedule:
    # daily, 02:00 UTC+8
    - cron: '0 18 * * *'
  workflow_dispatch:
  # No push trigger: the action commits with a PAT, and PAT commits re-trigger
  # push workflows — that combination caused an infinite run loop.

jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          # Keep only the sections that return real data;
          # commit/hour and day-of-week stats have been empty for years.
          SHOW_UPDATED_DATE: "True"
          SHOW_COMMIT: "False"
          SHOW_DAYS_OF_WEEK: "False"
          SHOW_TIMEZONE: "False"
          SHOW_SHORT_INFO: "False"
          SHOW_PROFILE_VIEWS: "False"
          SHOW_LINES_OF_CODE: "False"
          SHOW_LOC_CHART: "False"
          SHOW_TOTAL_CODE_TIME: "False"
          SHOW_LANGUAGE: "True"
          SHOW_OS: "True"
          SHOW_EDITORS: "True"
          SHOW_PROJECTS: "False"
          SHOW_LANGUAGE_PER_REPO: "False"
          LOCALE: "en"
          SHOW_AI_CODE_TIME: "False"
